import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nikolaindustry.iot.renderer',
  appName: 'Standalone IoT Renderer',
  webDir: 'dist'
};

export default config;
